import { PlaceholderScreen } from '@/components/ui/PlaceholderScreen';

export function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      description="Phase 8 will add profile editing, connected accounts, sign out, legal links, and account deletion."
    />
  );
}
