import { PlaceholderScreen } from '@/components/ui/PlaceholderScreen';

export function ProfileScreen() {
  return (
    <PlaceholderScreen
      title="Profile"
      description="Phase 7 will add identity, stats, recent reviews, top-rated items, and created lists."
    />
  );
}
