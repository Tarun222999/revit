# Media Components

Reusable media display components live here.

Examples:

- `MediaPoster`
- `MediaRow`
- `RatingBadge`
- `StatusBadge`

Keep these components generic enough to work across movies, series, anime, and later games.
